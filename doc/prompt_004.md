Generate a complete Kotlin Jetpack Compose project structure for the MASSAGER app.

Include:
- Navigation between screens (Login → Sign Up → Home).
- ViewModel for managing login and device data.
- Simple local state (simulate device list with mock data).
- Data classes:
  - User(email, password, token)
  - Device(name, type, status)
- Navigation setup using NavHostController.
- Use MVVM pattern with basic state handling.

Screens:
- LoginScreen
- SignUpScreen
- HomeScreen
- (Future placeholder: DeviceControlScreen)

Add light/dark theme toggle support.
